To Import Songs and Artists into Mongo
Make sure your instance of MongoDB is running (you can run it using the 'mongod' command in the terminal and then running 'mongo' in another terminal)
Once you have mongo running run 'mongo songsgql < songsJson.js' then 'mongo songsgql < artistsJson.js'